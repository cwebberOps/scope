<% @page_title = "New Term" %>

<%= link_to("<< Back to List", {:action => 'list'}, :class => 'back-link') %>

<div class="lookup new">
  <h2><%= @page_title %></h2>
  <%= form_for(:term, :url => {:action => 'create'}) do |f| %>

    <%= render(:partial => 'form', :locals => {:f => f}) %>

    <div class="form-buttons">
      <%= submit_tag("Create #{@page_title}") %>
    </div>
  <% end %>
</div>
